{% extends "squelette.html.twig" %}

{% block content %}

	{% if (error != "") %}
	    <div class="alert alert-error">
	    	{{ error }}
	    </div>
    {% endif %}
    
    <div align="right">
	    <form class="form-search">
	    	<input type="text" id="searchText" onkeyup="filterTable('#searchText','.campagneTable')"  class="input-medium search-query">
	    </form>
    </div>
    <div class="categorie">
		<a data-toggle="collapse" data-target="#current">
		Parties en activités
		</a>
	</div>
	<div id="current" class="collapse in">
	    <table class="table table-striped table-hover campagneTable">
			<thead>
				<tr>
					<th colspan="6">Partie (MJ)<input type="hidden" class="filterIndex" value="HEADER"></th>
				</tr>
				<tr>
					<th width="40px"></th>
					<th width="30%">Nom de la campagne</th>
					<th>Univers de la campagne</th>
					<th>Système de jeu</th>
					<th>Nb Joueurs</th>
					<th width="230px"><input type="hidden" class="filterIndex" value="HEADER"></th>
				</tr>
			</thead>
			<tbody>
			{% for campagne in campagnes %}
				<tr>
					<td>{% if campagne.activity > 0 %} <i class="icon-asterisk"></i> {% endif %}</td>
					<td><a href="{{ path('forum_campagne', {campagne_id: campagne.id}) }}">{{ campagne.name }}
						<br><img src="{{ campagne.banniere }}" width="150px" style="max-width: 150px"/></a></td>
					<td>{{ campagne.univers }}</td>
					<td>{{ campagne.systeme }}</td>
					<td>{{ campagne.nb_joueurs_actuel }} / {{ campagne.nb_joueurs }}</td>
					<td>
					<a href="{{ path('campagne', {id: campagne.id}) }}" class="btn iconeBtn" title="Détail"><i class="icon-search"></i></a>
					<a href="{{ path('forum_campagne', {campagne_id: campagne.id}) }}" class="btn iconeBtn" title="Forum"><i class="icon-globe/i></a>
					<a href="{{ path('perso_pnj', {campagne_id: campagne.id}) }}" class="btn iconeBtn" title="Galerie des PNJ"><i class="icon-folder-close"></i></a>
					<a href="{{ path('campagne_config_edit',  {campagne_id : campagne.id}) }}" title="Configurer l'espace campagne" class="btn iconeBtn"><i class="icon-wrench"></i></a>
					<a href="{{ path('campagne_edit', {id : campagne.id}) }}" class="btn iconeBtn btn-primary" title="Editer"><i class="icon-edit"></i></a>
					<input type="hidden" class="filterIndex" value="{{ campagne.name }}{{ campagne.univers }}{{ campagne.systeme }}">
					</td>
				</tr>
			{% endfor %}
			</tbody>
		</table>
	
		<table class="table table-striped table-hover campagneTable">
			<thead>
				<tr>
					<th colspan="6">Partie (Joueur)<input type="hidden" class="filterIndex" value="HEADER"></th>
				</tr>
				<tr>
					<th width="40px"></th>
					<th width="30%">Nom de la campagne</th>
					<th>Univers de la campagne</th>
					<th>Système de jeu</th>
					<th>MJ</th>
					<th width="230px"><input type="hidden" class="filterIndex" value="HEADER"></th>
				</tr>
			</thead>
			<tbody>
			{% for campagne in campagnes_pj %}
				<tr>
					<td>{% if campagne.activity > 0 %} <i class="icon-asterisk"></i> {% endif %}</td>
					<td><a href="{{ path('forum_campagne', {campagne_id: campagne.id}) }}">{{ campagne.name }}
						<br><img src="{{ campagne.banniere }}" width="150px" style="max-width: 150px"/></a></td>
					<td>{{ campagne.univers }}</td>
					<td>{{ campagne.systeme }}</td>
					<td><a href="{{ path('profile', {username : campagne.username}) }}">{{ campagne.username }}</a></td>
					<td>
						<a href="{{ path('campagne', {id: campagne.id}) }}" class="btn iconeBtn" title="Détail"><i class="icon-search"></i></a>
						<a href="{{ path('perso_edit', {campagne_id: campagne.id}) }}" title="Personnages" class="btn iconeBtn"><i class="icon-tasks"></i></a>
						<a href="{{ path('forum_campagne', {campagne_id: campagne.id}) }}" title="Forum" class="btn iconeBtn"><i class="icoicon-globe</a>
						<a href="{{ path('perso_pnj', {campagne_id: campagne.id}) }}" class="btn iconeBtn" title="Galerie des PNJ"><i class="icon-folder-close"></i></a>
						<a href="{{ path('campagne_quit', {id : campagne.id}) }}" title="Quitter" class="btn btn-danger iconeBtn"><i class="icon-remove"></i></a>
						<input type="hidden" class="filterIndex" value="{{ campagne.name }}{{ campagne.univers }}{{ campagne.systeme }}{{ campagne.username }}">
					</td>
				</tr>
			{% endfor %}
			</tbody>
		</table>
	</div>
	<div class="categorie">
		<a data-toggle="collapse" data-target="#archive">
		Archives
		</a>
	</div>
	<div id="archive" class="collapse out">
		
	    <table class="table table-striped table-hover campagneTable">
			<thead>
				<tr>
					<th colspan="6">Partie Archivé (MJ)<input type="hidden" class="filterIndex" value="HEADER"></th>
				</tr>
				<tr>
					<th width="40px"></th>
					<th width="30%">Nom de la campagne</th>
					<th>Univers de la campagne</th>
					<th>Système de jeu</th>
					<th>Nb Joueurs</th>
					<th width="230px"><input type="hidden" class="filterIndex" value="HEADER"></th>
				</tr>
			</thead>
			<tbody>
			{% for campagne in campagnes_mj_archive %}
				<tr>
					<td>{% if campagne.activity > 0 %} <i class="icon-asterisk"></i> {% endif %}</td>
					<td><a href="{{ path('forum_campagne', {campagne_id: campagne.id}) }}">{{ campagne.name }}
						<br><img src="{{ campagne.banniere }}" width="150px" style="max-width: 150px"/></a></td>
					<td>{{ campagne.univers }}</td>
					<td>{{ campagne.systeme }}</td>
					<td>{{ campagne.nb_joueurs_actuel }} / {{ campagne.nb_joueurs }}</td>
					<td>
					<a href="{{ path('campagne', {id: campagne.id}) }}" class="btn iconeBtn" title="Détail"><i class="icon-search"></i></a>
					<a href="{{ path('forum_campagne', {campagne_id: campagne.id}) }}" class="btn iconeBtn" title="Forum"><i class="icon-gicon-globe>
					<a href="{{ path('perso_pnj', {campagne_id: campagne.id}) }}" class="btn iconeBtn" title="Galerie des PNJ"><i class="icon-folder-close"></i></a>
					<a href="{{ path('campagne_config_edit',  {campagne_id : campagne.id}) }}" title="Configurer l'espace campagne" class="btn iconeBtn"><i class="icon-wrench"></i></a>
					<a href="{{ path('campagne_edit', {id : campagne.id}) }}" class="btn iconeBtn btn-primary" title="Editer"><i class="icon-edit"></i></a>
					<input type="hidden" class="filterIndex" value="{{ campagne.name }}{{ campagne.univers }}{{ campagne.systeme }}">
					</td>
				</tr>
			{% endfor %}
			</tbody>
		</table>
		
		<table class="table table-striped table-hover campagneTable">
			<thead>
				<tr>
					<th colspan="6">Partie archivé (Joueur)<input type="hidden" class="filterIndex" value="HEADER"></th>
				</tr>
				<tr>
					<th width="40px"></th>
					<th width="30%">Nom de la campagne</th>
					<th>Univers de la campagne</th>
					<th>Système de jeu</th>
					<th>MJ</th>
					<th width="230px"><input type="hidden" class="filterIndex" value="HEADER"></th>
				</tr>
			</thead>
			<tbody>
			{% for campagne in campagnes_pj_archive %}
				<tr>
					<td>{% if campagne.activity > 0 %} <i class="icon-asterisk"></i> {% endif %}</td>
					<td><a href="{{ path('forum_campagne', {campagne_id: campagne.id}) }}">{{ campagne.name }}
						<br><img src="{{ campagne.banniere }}" width="150px" style="max-width: 150px"/></a></td>
					<td>{{ campagne.univers }}</td>
					<td>{{ campagne.systeme }}</td>
					<td><a href="{{ path('profile', {username : campagne.username}) }}">{{ campagne.username }}</a></td>
					<td>
						<a href="{{ path('campagne', {id: campagne.id}) }}" class="btn iconeBtn" title="Détail"><i class="icon-search"></i></a>
						<a href="{{ path('perso_edit', {campagne_id: campagne.id}) }}" title="Personnages" class="btn iconeBtn"><i class="icon-tasks"></i></a>
						<a href="{{ path('forum_campagne', {campagne_id: campagne.id}) }}" title="Forum" class="btn iconeBtn"><i class="icon-globicon-globe						<a href="{{ path('perso_pnj', {campagne_id: campagne.id}) }}" class="btn iconeBtn" title="Galerie des PNJ"><i class="icon-folder-close"></i></a>
						<a href="{{ path('campagne_quit', {id : campagne.id}) }}" title="Quitter" class="btn btn-danger iconeBtn"><i class="icon-remove"></i></a>
						<input type="hidden" class="filterIndex" value="{{ campagne.name }}{{ campagne.univers }}{{ campagne.systeme }}{{ campagne.username }}">
					</td>
				</tr>
			{% endfor %}
			</tbody>
		</table>
	</div>

{% endblock %}