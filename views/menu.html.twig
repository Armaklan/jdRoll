	<div class="navbar navbar-inverse">
      <div class="navbar-inner">
        <div class="container">
            <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
		    <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
		    <span class="icon-bar"></span>
		    <span class="icon-bar"></span>
		    <span class="icon-bar"></span>
		    </a>

          <a class="brand" href="{{ path('homepage') }}">jdRoll</a>
          <div class="nav-collapse collapse">


            <ul class="nav">
              <li><a href="{{ path('homepage') }}"><i class="icon-home"></i> Accueil</a></li>
              <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-user"></i> Profil <b class="caret"></b></a>
                  <ul class="dropdown-menu">
              		<li><a href="{{ path('my_profile') }}">Editer mon Profil</a></li>
              		<li><a href="{{ path('abs') }}">Signaler une absence</a></li>
              	  </ul>
              </li>
              <li><a href="{{ path('messagerie') }}" id="msgTitle"
				  {% if nb_msg > 0 %} style='color: #FCFFA6' {% endif %}
				  ><i class="icon-envelope"></i>
				  	Messagerie ({{nb_msg}})</a></li>
              <li class="dropdown">
              	<a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-play-circle"></i> Jouer <b class="caret"></b></a>
              	<ul class="dropdown-menu">
					<li><a href="{{ path('campagne_my_list') }}">Mes campagnes</a></li>
					<li class="divider"></li>
					<li><a href="{{ path('campagne_list') }}">Rejoindre une campagne</a></li>
					<li><a href="{{ path('campagne_new') }}">Créer une campagne</a></li>
					<li class="divider"></li>
					<li><a href="{{ path('campagne_list_all') }}">Toutes les campagnes en-cours</a></li>
					<li><a href="{{ path('campagne_list_archive') }}">Toutes les campagnes archivées</a></li>
					{% if is_admin %}
						<li><a href="{{ path('campagne_list_prepa') }}">Toutes les campagnes en préparation</a></li>
					{% endif %}
					<li class="divider"></li>
					<li><a href="{{ path('user_list') }}">Liste des utilisateurs</a></li>
              	</ul>
              </li>
              <li><a href="{{ path('forum') }}"><i class="icon-folder-open"></i> Forum</a></li>
              <li class="dropdown">
				  <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-question-sign"></i> Aide <b class="caret"></b></a>
				  <ul class="dropdown-menu">
					  <li><a href="{{ path('static', {page: 'guide'} ) }}">Guide</a></li>
						<li><a href="{{ path('static', {page : 'faq'} ) }}">FAQ</a></li>
						<li class="divider"></li>
						<li><a href="{{ path('about') }}">A propos de jdRoll</a></li>
                        <li class="divider"></li>
                        <li><a href="{{ path('stat') }}">Quelques statistiques</a></li>
				  </ul>
			  </li>
            </ul>
            <ul class="nav pull-right">
                <li>
                    {% if (app.session.get('user') == null)  %}
                            <a href="{{ path('login_page', {url : '!'})  }}" class="navbar-link">Se connecter</a>

                    {% else %}
                            <a href="{{ path('logout')  }}" class="navbar-link">Se déconnecter</a>
                    {% endif %}
                </li>
            </ul>

          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>


