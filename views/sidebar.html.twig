
	<div align="center">
		Des remarques, des idées, venez les 
		<a href="http://www.armaklan.fr/mediawiki/index.php?title=JdRoll" target="_blank">inscrire par ici</a>
	</div>
	{% if (app.session.get('user') == null)  %}
	<form role="form" class="" action="{{ path('login_connect')  }}" method="POST">
	    <div class="form-group">
		    <div class="form-control">
		    	<input type="text" id="login" name="login" placeholder="Nom d'utilisateur">
		    </div>
	    </div>
	    <div class="form-group">
		    <div class="form-control">
		    	<input type="password" id="pass" name="pass" placeholder="Mot de passe">
		    </div>
	    </div>	    
	    <div class="form-group">
		    <div class="form-control">
			    <button type="submit" class="btn btn-primary">Se connecter</button>
			    <a href="{{ path('subscribe') }}" class="btn">S'inscrire</a>
		    </div>
	    </div>
    </form>
    {% else %}
    	<div align="center">
    		<img src="{{ app.session.get('user')['avatar'] }}" width="150px" class="img-rounded" />
    	</div>
    {% endif %}
    <p></p>
    	<ul class="nav nav-tabs nav-stacked">
		<li class='active'><a href='#'><b>Partie active (MJ)</b></a></li>
		{% for campagne in active_campagnes %}
			<li><a href="{{ path('campagne', {id : campagne.id}) }}">{{ campagne.name }}</a></li>
		{% endfor %}
	</ul>

	<ul class="nav nav-tabs nav-stacked">
		<li class='active'><a href='#'><b>Partie active (PJ)</b></a></li>
		{% for campagne in active_pj_campagnes %}
			<li><a href="{{ path('campagne', {id : campagne.id}) }}">{{ campagne.name }}</a></li>
		{% endfor %}
	</ul>
    