<script type='text/javascript'>
$(function() {


	var lastv;
	initFDP($('#zoneFicheCustomPopUp'),false);

	$('#zoneFicheCustomPopUp .editable').editable('toggleDisabled');

});


</script>

		{{ render(url("campagne_config", {id : campagne_id}) ) }}

 <style type='text/css'>

.ui-layout-center {

	position: relative;
	font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
	font-size: 14px;
	color: #333333

}

.ui-dialog {
	top: 80px !important;
}


.JDRollDroppedUserContro {
       z-index:1000;
}

.JDRollUserControl {
		display: inline-block;
}

</style>
<div id="persoModal" class="modal fade">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <span class="pull-right btn-bar"></span>
			    <h4 class="modal-title">Fiche de Personnages</h3>
            <div class="modal-body">


                <div class="form-group" id='zoneFichepopUp'>
                {% if perso.id != null %}
                    <div>
                    {% if perso.template_html is not empty %}
                        <div class="ui-layout-center" id='zoneFicheCustomPopUp'>

                            {% autoescape false %}{{ perso.template_html }}{% endautoescape %}
                            {% autoescape false %}{{ perso.template_fields }}{% endautoescape %}
                            {% autoescape false %}{{ perso.perso_fields }}{% endautoescape %}
                        </div>
                    {% else %}

                    <table class="table table-striped table-hover">
                        <tbody>
                        <tr></tr>
                        <tr>
                            <td></td>
                            <td>{% autoescape false %}{{ perso.technical }}{% endautoescape %}</td>
                        </tr>
                        </tbody>
                    </table>

				    </div>
			        {% endif %}


                {% endif %}
			    </div>

            </div>
		</div>
		</div>
	</div>
</div>
