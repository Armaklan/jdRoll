	<div class="col-xs-12 col-sm-12 visible-xs visible-sm sidebarTop">
		<div align="center">
			<h5>{{ campagne.name }}</h5>
		</div>
	    <div class="row">
	    	<div class="col-md-4">
	    		<div align="center">
    				<img src="{{ app.session.get('user')['avatar'] }}" width="130px" class="img-rounded" />
    			</div><p/>
    			    <div align="center">
    			    	<div class="btn-group">
	    			    	{% set pull_mode = "" %}
					        {% include("composant/my_campagne_btm.html.twig") %}
							<a href="{{ path('forum_campagne', {campagne_id: campagne_id}) }}" class="btn btn-default iconeBtn sidebarBtn" title="Forum"><i class="icon-folder-close"></i></a>
							<a href="{{ path('perso_pnj', {campagne_id: campagne_id}) }}" class="btn btn-default iconeBtn sidebarBtn" title="Galerie des PNJ"><i class="icon-group"></i></a>
							<a href="#dicerModal" role="button" data-toggle="modal" class="btn btn-default iconeBtn sidebarBtn" title="Lanceur de dés"><i class="icon-cogs"></i></a>
	                        <a href="#notesModal" role="button" data-toggle="modal" class="btn btn-default iconeBtn sidebarBtn" title="Bloc-Notes"><i class="icon-book"></i></a>
	                        <a class="btn btn-default iconeBtn sidebarBtn" onClick="alarm({{app.session.get('user')['id']}}, {{campagne.id}})"
	                            title="Mettre une alerte">
	                            <i class="alarm-pj icon-bell-alt {% if alert %}alarm-on{% endif %}"></i>
	                        </a>
	                    </div>
				    </div><p/>
	    	</div>
	    	<div class="col-md-4">
				<ul class="nav nav-stacked">
					<li><b>Personnages</b></li>
					{% for perso in allPerso %}
						<li>
                            <a onClick="persoModalService.openPerso({{campagne_id}}, {{perso.id}})">
                                <img class="thumb_avatar" src="{{ path('homepage') }}files/thumbnails/perso_{{ perso.id }}.png"/>
                                {{ perso.name }} ({{ perso.username }})
                            </a>
                        </li>
					{% endfor %}
				</ul>
			</div>
			<div class="col-md-4" style="padding-left: 1.3em; padding-right: 1.3em;">
				<div class="page-header">
				  <h5>Informations campagnes</h5>
				</div>
				{% if config.sidebar_text != "" %}
					{% autoescape false %}
						{{ config.sidebar_text }}
					{% endautoescape %}
				{% endif %}
			</div>
	    </div>
	</div>



<div class="col-md-3 col-lg-2 sidebar hidden-xs hidden-sm maxheight">

	<div id="tabs-panel">
	    <ul class="nav nav-tabs nav-justified">
	      	<li class="active"><a href="#campagne" data-toggle="tab"><i class="icon-bookmark"></i> Campagne</a></li>
	  	 	<li><a href="#general" data-toggle="tab"><i class="icon-globe"></i> Général</a></li>
	    </ul>
	</div>

	<div class="tab-content">

		<div class="tab-pane active" id="campagne">
			<div align="center">
				<h5>{{ campagne.name }}</h5>
			</div>

			<div id="widgets-panel">
		          <div align="center">
					<div class="btn-group btn-group-sm">
			    	{% set pull_mode = "pull-right" %}
			    	<a class="btn btn-default iconeBtn sidebarBtn" onClick="alarm({{app.session.get('user')['id']}}, {{campagne.id}})"
			            title="Mettre une alerte">
			            <i class="alarm-pj icon-bell-alt {% if alert %}alarm-on{% endif %}"></i>
			        </a>
					<a href="{{ path('forum_campagne', {campagne_id: campagne_id}) }}" class="btn btn-default iconeBtn sidebarBtn" title="Forum"><i class="icon-folder-close"></i></a>
					<a href="{{ path('perso_pnj', {campagne_id: campagne_id}) }}" class="btn btn-default iconeBtn sidebarBtn" title="Galerie des PNJ"><i class="icon-group"></i></a>
					<a href="#dicerModal" role="button" data-toggle="modal" class="btn btn-default iconeBtn sidebarBtn" title="Lanceur de dés"><i class="icon-cogs"></i></a>
			        <a href="#notesModal" role="button" data-toggle="modal" class="btn btn-default iconeBtn sidebarBtn" title="Bloc-Notes"><i class="icon-book"></i></a>
			        </div>

		          </div>

		    </div>
		    <p/>
			{% include("composant/campagne_accordion.html.twig") %}
		</div>
		<div class="tab-pane" id="general">

		  <p/>
		  <div align="center">
			<img src="{{ app.session.get('user')['avatar'] }}" width="130px" class="img-rounded" />
		  </div>
		  <p/>

	      <div id="widgets-panel">

	          <div align="center">
	            <div class="btn-group btn-group-sm">
	            	<a href="{{ path('campagne_my_list') }}" title="Mes Campagnes" class="btn btn-default iconeBtn quickLink1"><i class="icon-bookmark"></i></a>
					{{ include("button/forum_btm.html.twig") }}
		            <a href="{{ path('messagerie') }}" title="Messagerie" class="btn btn-default iconeBtn quickLink3"><i class="icon-envelope"></i></a>
	            </div>
	          </div>
	          <p/>

	        {{ include("composant/sidebar_list_partie_std.html.twig") }}

		</div>
	</div>
</div>
