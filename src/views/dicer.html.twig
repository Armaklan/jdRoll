<div id="dicerModal" class="modal fade">
    <div class="modal-dialog modal-lg">
    	<div class="modal-content">
    		<div class="modal-header">
    		    	    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    		    	    <h3>Lanceur de dés <a href="#dicerHelp" role="button" data-toggle="modal" class="btn btn-link"><i class="icon-question"></i></a></h3>
    		        </div>
    		        <div class="modal-body">
    		        	    <form role="form" class="form col-xs-12">
								<div class="form-group col-md-2 col-xs-12">
                                    <label class="control-label" for="dicerParam">Formule</label>
                                    <input type="text" 
                                        class="form-control" 
                                        id="dicerParam" 
                                        name="dicerParam" 
                                        placeholder="Ex : 1d6 + 1d4" 
                                        value="{{ config.default_dice }}"
                                        required>
							    </div>
							    <div class="form-group col-md-4 col-xs-12">
                                    <label class="control-label" for="dicerDescription">Description</label>
							    	<input type="text" 
                                           class="form-control" 
                                           id="dicerDescription" 
                                           name="dicerDescription" 
                                           placeholder="Ex: Crocheter la serrure">
							    </div>
                                <div class="col-md-3 col-xs-12" style="padding-top: 26px">
                                    <div class="col-xs-10">
                                        <button type="submit" class="btn btn-default form-control" onClick="return uiDicer.dicerModalLaunch();">Lancer les dés</button>
                                    </div>
                                    <div class="col-xs-2">
                                        <img src="{{global.request.baseUrl}}/img/loading.gif" class="hide" id="waitingLaunch">
                                    </div>
                                </div>
								<div class="col-xs-12">
                                <span class="help-block">Consultez l'aide (?) pour plus de détail sur les formules possibles. <br>Les lancés effectués via cette popup ne crée par de post dans le sujet de jeu.</span><br>
								<center>
                                	<span id="resultatDicer"></span>
								</center>
                                </div>
                            </form>
    		    			    <table class="table table-striped table-hover" id="resultatDicerTable">
    		    					<thead>
    		    						<tr>
    		    							<th>Joueur</th>
    		    							<th>Date</th>
    		    							<th>Description</th>
    		    							<th>Result</th>
    		    						</tr>
    		    					</thead>
    		    					<tbody>
    		    					{% for jet in jets %}
    		    						<tr>
    		    							<td>{{ jet.username }}</td>
    		    							<td>{{ jet.create_date }}</td>
    		    							<td>{{ jet.description }}</td>
    		    							<td>{{ jet.result }}</td>
    		    						</tr>
    		    					{% endfor %}
    		    					</tbody>
    		    				</table>

    		        </div>
    		        <div class="modal-footer">
    		        	<button class="btn btn-default" data-dismiss="modal" aria-hidden="true">Fermer</button>
    		        </div>
    	</div>
    </div>
</div>
